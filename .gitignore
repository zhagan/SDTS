/target
/recordings/*.jsonl

# Generated by `make build` (see .github/workflows/pages.yml, which builds
# and deploys these fresh on push). scenarios/ and crates/sdts-wasm are the
# source of truth; nothing under here should be hand-edited or committed.
/docs/pkg/
/docs/scenarios/*.json
